{
	"name": "caroline",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"build": "npm run build:assets && tsc",
		"build:assets": "rm -rf static && cp -R public static",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"typescript": "5.8.3"
	}
}
